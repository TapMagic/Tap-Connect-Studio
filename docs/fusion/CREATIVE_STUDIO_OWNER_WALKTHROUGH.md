# Creative Studio Owner Walkthrough

Branch: `tapconnect-creative-studio-rescue`  
**Classification:** `IMPLEMENTATION IN PROGRESS`

Automated route checks are evidence that controls can be reached; they are not a
substitute for an Owner completing the workflow, judging the result, and approving it.

## Current walkthrough truth

| Workflow | Current evidence | Honest status |
|---|---|---|
| Open `/dashboard/card/edit`, switch Edit/Preview, exit safely | Automated route coverage | Verified by automation in this closeout only if the final run passes |
| Open Appearance, Selection, and Composition sliding inspectors | Automated route coverage and prior local video | Manual focus, keyboard, and visual calibration still open |
| Add composition image/frame/divider; select masks; layer/group | Automated route coverage | Does not exhaust every control combination or prove saved persistence |
| Open Media and Asset Browser from composition image | Automated route coverage | Source tabs are proven visible; live Pexels/Logo.dev/R2 behavior is not |
| Create typed gradient and choose pattern/texture | Automated route coverage | Basic route and unit proof only; visual and contrast review remains |
| Adjust frame stroke and search masks | Automated route coverage | Basic route proof only; scale/alignment calibration remains |
| Preview at tablet/phone sizes | Automated browser simulation | Not a physical-device review |
| Generate/update/revoke Live Device draft | Automated token/route coverage | Physical-phone fidelity re-scan remains open |
| Use Pexels | Unavailable state only | `PEXELS_API_KEY` is missing |
| Use Logo.dev | Proxy contract/unit mock only | `LOGO_DEV_TOKEN` is missing |
| Import provider media durably | Unavailable state only | R2 credentials are missing |
| Save/reuse a style, composition, or editable template | No Owner workflow | `NOT IMPLEMENTED` |
| Build structured text/image wrapping | No Owner workflow | `NOT IMPLEMENTED` |

## Manual Owner steps still required

1. Review the editor at large desktop, laptop, tablet portrait/landscape, and phone-sized
   review widths for clipping, overlap, panel clearance, and usable touch targets.
2. Complete a keyboard-only pass through the inspector, media browser, gradient studio,
   mask browser, layer controls, Preview, and Live Device.
3. Complete a VoiceOver pass; Playwright role/name checks are not a screen-reader pass.
4. Scan a fresh HTTPS Live Device QR on a physical phone and compare alignment, text
   wrapping, layers, fonts, masks, and backgrounds with Studio.
5. In an isolated non-production environment with approved credentials, exercise live
   Pexels, Logo.dev, and R2 import, including failure, rate-limit, attribution, and
   persistence behavior.

## Existing local evidence

Evidence under `tmp/creative-studio-rescue/` is gitignored and may describe an earlier
HEAD. It must not be treated as final-HEAD proof unless regenerated by the closeout run.
